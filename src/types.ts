export interface ProductCardData {
  id: string;
  imageUrl: string;
  title: string;
  description: string;
}

export interface ProductDetailData {
  title: string;
  introText: string;
  appTip: string;
  ingredients: string;
}
